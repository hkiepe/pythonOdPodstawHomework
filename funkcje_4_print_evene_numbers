def even_number(number):
    if (number % 2) == 0:
        return True
    else:
        return False

num_list = [1, 2, 3, 4]

for number in num_list:
    if even_number(number) == True:
        print(number)
